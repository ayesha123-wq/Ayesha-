:root {
  --lavender: #E6E6FA;
  --dark-purple: #3A0B5F;
  --muted: rgba(58,11,95,0.08);
  --glass: rgba(255,255,255,0.6);
  --radius: 16px;
}

* { box-sizing: border-box; }
html, body {
  margin: 0;
  font-family: 'Poppins', sans-serif;
  background: var(--lavender);
  color: var(--dark-purple);
}

.container { max-width: 1100px; margin: 36px auto; padding: 28px; }

header { display: flex; justify-content: space-between; align-items: center; }
.brand { display: flex; align-items: center; gap: 12px; }
.logo { width: 56px; height: 56px; background: var(--muted); border-radius: 12px; display: flex; align-items: center; justify-content: center; font-weight: 700; color: var(--dark-purple); }
h1, h2, h3 { font-family: 'Great Vibes', cursive; margin: 0; }
nav { display: flex; gap: 12px; }
nav a { text-decoration: none; padding: 8px 12px; border-radius: 10px; color: var(--dark-purple); font-weight: 600; }
nav a:hover { background: var(--glass); }

.hero { display: grid; grid-template-columns: 1fr 300px; gap: 24px; margin-top: 26px; }
.hero-card { background: rgba(255,255,255,0.7); border-radius: var(--radius); padding: 26px; box-shadow: 0 8px 30px rgba(58,11,95,0.06); }
.avatar { width: 100%; height: 250px; border-radius: 12px; background: var(--muted); display: flex; align-items: center; justify-content: center; font-weight: 700; }
.projects { display: grid; grid-template-columns: repeat(auto-fit,minmax(220px,1fr)); gap: 18px; margin-top: 28px; }
.project { background: rgba(255,255,255,0.7); padding: 16px; border-radius: 12px; border: 1px solid rgba(58,11,95,0.04); }

.two-col { display: grid; grid-template-columns: 1fr 1fr; gap: 18px; margin-top: 26px; }
.contact form { display: flex; flex-direction: column; gap: 10px; }
input, textarea, button { padding: 10px; border-radius: 10px; border: 1px solid rgba(58,11,95,0.08); font-family: inherit; }
button { background: var(--dark-purple); color: #fff; border: none; cursor: pointer; }

footer { text-align: center; margin: 32px 0 20px; opacity: 0.8; }

.muted { opacity: 0.8; font-size: 14px; }
.btn-outline { background: transparent; border: 1px solid rgba(58,11,95,0.08); padding: 8px 12px; border-radius: 10px; cursor: pointer; }

@media (max-width: 900px) {
  .hero { grid-template-columns: 1fr; }
  .two-col { grid-template-columns: 1fr; }
  nav { display: none; }
}
